[project]
name = "bidframe"
version = "0.1.0"
description = "Consensus security-deposit settlement on GenLayer"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
